import styled from "styled-components"

export const Container = styled.header`
    width: 100%;
    display: flex;
    background-color: var(--color-primary);
    height: 3rem;
    color: var(--color-white);
    border-bottom: 2px solid  rgb(109, 8, 62);
    justify-content: center;
    align-items :center;
`